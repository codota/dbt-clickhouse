---
name: "test_job"

on:  # yamllint disable-line rule:truthy
  workflow_call:
    inputs:
      python-version:
        required: true
        type: string
      clickhouse-version:
        required: true
        type: string

jobs:
  test:
    name: Python ${{ inputs.python-version }} | Clickhouse ${{ inputs.clickhouse-version }} | Ubuntu
    runs-on: ubuntu-stepsecurity-x64
    timeout-minutes: 45
    env:
      TEST_SETTINGS_FILE: latest

    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0

      - name: Set environment variables
        run: |
          echo "DBT_CH_TEST_CH_VERSION=${{ inputs.clickhouse-version }}" >> $GITHUB_ENV

          # Set DBT_CH_TEST_SETTINGS based on major version
          MAJOR_VERSION=$(echo "${{ inputs.clickhouse-version }}" | cut -d. -f1)
          if [ "$MAJOR_VERSION" -eq 23 ]; then
            echo "DBT_CH_TEST_SETTINGS=23" >> $GITHUB_ENV
          elif [ "$MAJOR_VERSION" -eq 24 ]; then
            echo "DBT_CH_TEST_SETTINGS=24" >> $GITHUB_ENV
          else
            echo "DBT_CH_TEST_SETTINGS=latest" >> $GITHUB_ENV
          fi

      - name: Set up Docker Compose
        uses: docker/setup-compose-action@4eb059ff7f16592f9c84d5ca339c53cb7c5064e2 # v2.3.0
        with:
          version: v2.36.0

      - name: Run ClickHouse Cluster Containers
        env:
          PROJECT_ROOT: ${{ github.workspace }}/tests/integration
        run: REPLICA_NUM=1 docker compose -f ${{ github.workspace }}/tests/integration/docker-compose.yml up -d

      - name: Setup Python ${{ inputs.python-version }}
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: ${{ inputs.python-version }}

      - name: Install requirements
        run: pip3 install . -r dev_requirements.txt

      - name: Run HTTP tests
        env:
          DBT_CH_TEST_CLUSTER: test_shard
        run: |
          PYTHONPATH="${PYTHONPATH}:dbt"
          pytest tests --timeout=120

      - name: Run Native tests
        env:
          DBT_CH_TEST_PORT: 9000
          DBT_CH_TEST_CLUSTER: test_shard
        run: |
          PYTHONPATH="${PYTHONPATH}:dbt"
          pytest tests --timeout=120
